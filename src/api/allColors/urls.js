import { baseUrl } from "../hendleApi";

const urls = {
  background: `${baseUrl}/background`,
  colors: `${baseUrl}/colors`,
  DeleteBackground: `${baseUrl}/DeleteBackground`,

};

export default urls;


// /DeleteBackground/:id